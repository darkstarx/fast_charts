name: fast_charts
description: A package of widgets for representing horizontal and vertical stacked bar charts.
version: 0.0.2
repository: https://github.com/darkstarx/fast_charts

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
